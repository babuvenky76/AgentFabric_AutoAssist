global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'autoassist-monitor'

scrape_configs:
  - job_name: 'autoassist'
    metrics_path: '/metrics/prometheus'
    static_configs:
      - targets: ['autoassist-api:8000']  # Docker Compose service name
